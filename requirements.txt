### pip file format >=7.0
pip>=7.0

## Django 1.9+ and utils
Django>=1.9
docutils>=0.12

## django-compressor for sass compiling and ES6 compiling
-r ./requirements_windows.txt ; sys_platform in 'win32 win64'
    # If you are running windows, then you need to install these requirements
    # seperately, since they need seperate install flags.
django-compressor>=2.0
django-compressor-toolkit==0.5.0
